#ifndef BOTANISTE_CPP
#define BOTANISTE_CPP

#include <iostream>
#include <string>
#include <vector>

#include "Plante.h"
//#include "Botaniste.h"
//#include "PlanteCarnivore.h"

/*

    void Botaniste::afficher() {
    
        std::cout << " Tu as " << _argent << " € " << std::endl;
        std::cout << _engrais << " d'engrais" << std::endl;
    }

    void Botaniste::dormir(){
    
        std::cout << " tu dors tes endormi. " <<std::endl;
    }

    void Botaniste::acheterEngrais() {
    
        if (_argent > 19) {
            
            std::cout << " tu viens d'acheter de l'engrais " << std::endl;
            _argent -= 20;
            _engrais +=20;
        }
    }
*/

#endif


 
/*

    void Mage::affichePartie(){
        std::cout << _name << " possède " << _pointsDeVie << " points de vie, " << _mana <<
        "/" << _manaTotal << " poins de mana.";
        _hand[0]->affiche();
    }
    
    Mage::Mage(int pv, std::string nom, std::vector<Carte*> cartesEnMain):
    _pointsDeVie(pv), _name(nom), _manaTotal(0), _mana(0), _hand(cartesEnMain) {}
    
    void Mage::playCard(int indexDansLaMain){
        _board.push_back(_hand[indexDansLaMain]);      
    }
    
    void Mage::regainMana(){
        _mana=_manaTotal;
    }

*/

